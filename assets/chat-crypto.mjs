const enc=new TextEncoder(),dec=new TextDecoder();
export async function generateRoomKey(){return crypto.subtle.generateKey({name:"AES-GCM",length:256},true,["encrypt","decrypt"])}
export async function exportKey(key){const raw=await crypto.subtle.exportKey("raw",key);return btoa(String.fromCharCode(...new Uint8Array(raw)))}
export async function importKey(s){const raw=Uint8Array.from(atob(s),c=>c.charCodeAt(0));return crypto.subtle.importKey("raw",raw,{name:"AES-GCM"},false,["encrypt","decrypt"])}
export async function encryptText(key,text){const iv=crypto.getRandomValues(new Uint8Array(12));const data=await crypto.subtle.encrypt({name:"AES-GCM",iv},key,enc.encode(text));return{iv:btoa(String.fromCharCode(...iv)),data:btoa(String.fromCharCode(...new Uint8Array(data)))}}
export async function decryptText(key,payload){const iv=Uint8Array.from(atob(payload.iv),c=>c.charCodeAt(0)),data=Uint8Array.from(atob(payload.data),c=>c.charCodeAt(0));return dec.decode(await crypto.subtle.decrypt({name:"AES-GCM",iv},key,data))}
export async function encryptBlob(key,blob){const iv=crypto.getRandomValues(new Uint8Array(12)),data=await crypto.subtle.encrypt({name:"AES-GCM",iv},key,await blob.arrayBuffer());return{iv,data:new Blob([data],{type:"application/octet-stream"})}}

const DEVICE_DB="secure_chat_device_v1",DEVICE_STORE="keys",DEVICE_KEY="private_key";
function openDeviceDb(){return new Promise((resolve,reject)=>{const req=indexedDB.open(DEVICE_DB,1);req.onupgradeneeded=()=>{if(!req.result.objectStoreNames.contains(DEVICE_STORE))req.result.createObjectStore(DEVICE_STORE)};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error||new Error("device key storage unavailable"))})}
export async function loadDevicePrivateKey(){const db=await openDeviceDb();return new Promise((resolve,reject)=>{const tx=db.transaction(DEVICE_STORE,"readonly"),req=tx.objectStore(DEVICE_STORE).get(DEVICE_KEY);req.onsuccess=()=>{db.close();resolve(req.result||null)};req.onerror=()=>{db.close();reject(req.error)}})}
export async function saveDevicePrivateKey(key){const db=await openDeviceDb();return new Promise((resolve,reject)=>{const tx=db.transaction(DEVICE_STORE,"readwrite");tx.objectStore(DEVICE_STORE).put(key,DEVICE_KEY);tx.oncomplete=()=>{db.close();resolve()};tx.onerror=()=>{db.close();reject(tx.error||new Error("device key storage failed"))}})}
export async function clearDevicePrivateKey(){try{const db=await openDeviceDb();await new Promise((resolve,reject)=>{const tx=db.transaction(DEVICE_STORE,"readwrite");tx.objectStore(DEVICE_STORE).delete(DEVICE_KEY);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)});db.close()}catch{}}

export async function generateDeviceKeyPair(){const pair=await crypto.subtle.generateKey({name:"ECDH",namedCurve:"P-256"},true,["deriveBits"]);const pub=await crypto.subtle.exportKey("jwk",pair.publicKey);const priv=await crypto.subtle.exportKey("jwk",pair.privateKey);const locked=await crypto.subtle.importKey("jwk",priv,{name:"ECDH",namedCurve:"P-256"},false,["deriveBits"]);return{publicKey:pair.publicKey,privateKey:locked,publicJwk:pub}}
export async function exportPublicKey(key){return crypto.subtle.exportKey("jwk",key)}
export async function exportPrivateKey(key){return crypto.subtle.exportKey("jwk",key)}
export async function importPublicKey(jwk){return crypto.subtle.importKey("jwk",jwk,{name:"ECDH",namedCurve:"P-256"},true,[])}
export async function importPrivateKey(jwk){return crypto.subtle.importKey("jwk",jwk,{name:"ECDH",namedCurve:"P-256"},false,["deriveBits"])}
export async function wrapRoomKey(devicePublicKey,roomKey){const eph=await generateDeviceKeyPair();const bits=await crypto.subtle.deriveBits({name:"ECDH",public:devicePublicKey},eph.privateKey,256);const wrap=await crypto.subtle.importKey("raw",bits,{name:"AES-GCM"},false,["encrypt","decrypt"]);const raw=await crypto.subtle.exportKey("raw",roomKey);const iv=crypto.getRandomValues(new Uint8Array(12));const data=await crypto.subtle.encrypt({name:"AES-GCM",iv},wrap,raw);return{ephemeralPublicKey:await exportPublicKey(eph.publicKey),iv:btoa(String.fromCharCode(...iv)),data:btoa(String.fromCharCode(...new Uint8Array(data)))}}
export async function unwrapRoomKey(devicePrivateKey,envelope){const eph=await importPublicKey(envelope.ephemeralPublicKey);const bits=await crypto.subtle.deriveBits({name:"ECDH",public:eph},devicePrivateKey,256);const wrap=await crypto.subtle.importKey("raw",bits,{name:"AES-GCM"},false,["decrypt"]);const iv=Uint8Array.from(atob(envelope.iv),c=>c.charCodeAt(0));const data=Uint8Array.from(atob(envelope.data),c=>c.charCodeAt(0));const raw=await crypto.subtle.decrypt({name:"AES-GCM",iv},wrap,data);return crypto.subtle.importKey("raw",raw,{name:"AES-GCM"},false,["encrypt","decrypt"])}

export async function encryptFile(key,file){if(file.size>100*1024*1024)throw new Error("文件不能超过 100MB");const iv=crypto.getRandomValues(new Uint8Array(12));const plain=await file.arrayBuffer();const data=await crypto.subtle.encrypt({name:"AES-GCM",iv},key,plain);return{iv:btoa(String.fromCharCode(...iv)),blob:new Blob([data],{type:"application/octet-stream"})}}
export async function decryptBlob(key,blob,ivEncoded,type){const iv=Uint8Array.from(atob(ivEncoded),x=>x.charCodeAt(0));const data=await blob.arrayBuffer();const plain=await crypto.subtle.decrypt({name:"AES-GCM",iv},key,data);return new Blob([plain],{type:type||"application/octet-stream"})}
export async function encryptFileChunked(key,file,chunkSize=2*1024*1024,onProgress=()=>{}){if(file.size>100*1024*1024)throw new Error("文件不能超过 100MB");const chunks=[];let offset=0;while(offset<file.size){const part=file.slice(offset,Math.min(offset+chunkSize,file.size));const enc=await encryptFile(key,part);chunks.push({iv:enc.iv,blob:enc.blob});offset+=part.size;onProgress(Math.round(offset/file.size*100))}return chunks}

const CHUNK_MAGIC=new TextEncoder().encode("SC01");
const CHUNK_VERSION=1;
export async function encryptFileStream(key,file,chunkSize=2*1024*1024,onProgress=()=>{}){if(file.size>100*1024*1024)throw new Error("文件不能超过 100MB");let offset=0;const body=new ReadableStream({async pull(controller){try{if(offset===0)controller.enqueue(CHUNK_MAGIC);if(offset>=file.size){controller.close();return}const part=file.slice(offset,Math.min(offset+chunkSize,file.size));const plain=await part.arrayBuffer(),iv=crypto.getRandomValues(new Uint8Array(12)),data=new Uint8Array(await crypto.subtle.encrypt({name:"AES-GCM",iv},key,plain));const frame=new Uint8Array(4+1+12+data.length);new DataView(frame.buffer).setUint32(0,plain.byteLength);frame[4]=CHUNK_VERSION;frame.set(iv,5);frame.set(data,17);controller.enqueue(frame);offset+=part.size;onProgress(Math.round(offset/file.size*100))}catch(e){controller.error(e)}}});return{body,type:"application/octet-stream",size:null,format:"chunked-v1"}}
export async function decryptChunkedBlob(key,blob,type){const raw=new Uint8Array(await blob.arrayBuffer());if(raw.length<4||new TextDecoder().decode(raw.slice(0,4))!=="SC01")throw new Error("unsupported encrypted media format");let pos=4;const parts=[];while(pos<raw.length){if(pos+17>raw.length)throw new Error("invalid encrypted media");const len=new DataView(raw.buffer,raw.byteOffset+pos,4).getUint32(0);if(raw[pos+4]!==CHUNK_VERSION)throw new Error("unsupported encrypted media version");const iv=raw.slice(pos+5,pos+17);const end=pos+17+len+16;if(end>raw.length)throw new Error("invalid encrypted media");const plain=await crypto.subtle.decrypt({name:"AES-GCM",iv},key,raw.slice(pos+17,end));parts.push(plain);pos=end}return new Blob(parts,{type:type||"application/octet-stream"})}
