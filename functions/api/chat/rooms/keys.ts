function json(data:any,status=200){return Response.json(data,{status,headers:{"cache-control":"no-store"}})}
async function auth(c:any){const h=c.request.headers.get("Cookie")||"";const t=h.match(/(?:^|;\s*)chat_session=([^;]+)/)?.[1];if(!t||!c.env.CHAT_SESSIONS)return null;const v=await c.env.CHAT_SESSIONS.get(t,"json");return v?.userId||v||null}
export async function onRequestPost(context:any){const uid=await auth(context);if(!uid)return json({error:"unauthorized"},401);const roomId=String(context.params.room||""),b=await context.request.json().catch(()=>null),deviceId=String(b?.deviceId||""),encryptedKey=String(b?.encryptedKey||"");if(!deviceId||!encryptedKey)return json({error:"invalid key"},400);const member=await context.env.DB.prepare("SELECT 1 FROM room_members WHERE room_id=? AND user_id=?").bind(roomId,uid).first();const device=await context.env.DB.prepare("SELECT 1 FROM devices WHERE id=? AND user_id=?").bind(deviceId,uid).first();if(!member||!device)return json({error:"forbidden"},403);await context.env.DB.prepare("INSERT OR REPLACE INTO room_keys(room_id,device_id,encrypted_key,created_at) VALUES(?,?,?,?)").bind(roomId,deviceId,encryptedKey,Date.now()).run();return json({ok:true});}
export async function onRequestGet(context:any){const uid=await auth(context);if(!uid)return json({error:"unauthorized"},401);const roomId=String(context.params.room||""),member=await context.env.DB.prepare("SELECT 1 FROM room_members WHERE room_id=? AND user_id=?").bind(roomId,uid).first();if(!member)return json({error:"forbidden"},403);const r=await context.env.DB.prepare("SELECT k.device_id,k.encrypted_key,d.public_key FROM room_keys k JOIN devices d ON d.id=k.device_id WHERE k.room_id=? AND d.user_id=?").bind(roomId,uid).all();return json({keys:r.results||[]});}
