function json(data:any,status=200){return Response.json(data,{status,headers:{"cache-control":"no-store"}})}
async function auth(c:any){const h=c.request.headers.get("Cookie")||"";const t=h.match(/(?:^|;\s*)chat_session=([^;]+)/)?.[1];if(!t||!c.env.CHAT_SESSIONS)return null;const v=await c.env.CHAT_SESSIONS.get(t,"json");return v?.userId||v||null}
export async function onRequestGet(context:any){const uid=await auth(context);if(!uid)return new Response("Unauthorized",{status:401});const roomId=String(context.params.room||"");const member=await context.env.DB.prepare("SELECT 1 FROM room_members m JOIN rooms r ON r.id=m.room_id WHERE m.room_id=? AND m.user_id=? AND r.expires_at>?" ).bind(roomId,uid,Date.now()).first();if(!member)return new Response("Forbidden",{status:403});const stub=context.env.CHAT_ROOMS.get(context.env.CHAT_ROOMS.idFromName(roomId));return stub.fetch(new Request("https://chat-room/ws",{method:"GET",headers:{"x-chat-user":uid}}));}
