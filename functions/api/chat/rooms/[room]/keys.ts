function json(data:any,status=200){return Response.json(data,{status,headers:{"cache-control":"no-store"}})}
async function sessionUserId(c:any){const cookie=c.request.headers.get("Cookie")||"";const t=c.match(/(?:^|;\s*)chat_session=([^;]+)/)?.[1];if(!t||!c.env.CHAT_SESSIONS)return null;const data=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(t));const hash=Array.from(new Uint8Array(data)).map(b=>b.toString(16).padStart(2,"0")).join("");return c.env.CHAT_SESSIONS.get("session:"+hash)}export async function onRequestPost(context:any){const uid=await sessionUserId(context);if(!uid||!context.env.DB)return json({error:"unauthorized"},401);const roomId=String(context.params.room||""),b=await context.request.json().catch(()=>null),deviceId=String(b?.deviceId||""),encryptedKey=String(b?.encryptedKey||"");if(!deviceId||!encryptedKey||encryptedKey.length>20000)return json({error:"invalid key"},400);const member=await context.env.DB.prepare("SELECT 1 FROM room_members m JOIN rooms r ON r.id=m.room_id WHERE m.room_id=? AND m.user_id=? AND r.expires_at>?").bind(roomId,uid,Date.now()).first();const device=await context.env.DB.prepare("SELECT 1 FROM devices WHERE id=? AND user_id=?").bind(deviceId,uid).first();if(!member||!device)return json({error:"forbidden"},403);await context.env.DB.prepare("INSERT OR REPLACE INTO room_keys(room_id,device_id,encrypted_key,created_at) VALUES(?,?,?,?)").bind(roomId,deviceId,encryptedKey,Date.now()).run();return json({ok:true})}
export async function onRequestGet(context:any){const uid=await sessionUserId(context);if(!uid||!context.env.DB)return json({error:"unauthorized"},401);const roomId=String(context.params.room||""),member=await context.env.DB.prepare("SELECT 1 FROM room_members m JOIN rooms r ON r.id=m.room_id WHERE m.room_id=? AND m.user_id=? AND r.expires_at>?").bind(roomId,uid,Date.now()).first();if(!member)return json({error:"forbidden"},403);const r=await context.env.DB.prepare("SELECT k.device_id,k.encrypted_key,d.public_key FROM room_keys k JOIN devices d ON d.id=k.device_id WHERE k.room_id=? AND d.user_id=?").bind(roomId,uid).all();return json({keys:r.results||[]})}
