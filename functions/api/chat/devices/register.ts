function json(data:any,status=200){return Response.json(data,{status,headers:{"cache-control":"no-store"}})}
async function auth(c:any){const h=c.request.headers.get("Cookie")||"";const t=h.match(/(?:^|;\s*)chat_session=([^;]+)/)?.[1];if(!t||!c.env.CHAT_SESSIONS)return null;const v=await c.env.CHAT_SESSIONS.get(t,"json");return v?.userId||v||null}
export async function onRequestPost(context:any){const uid=await auth(context);if(!uid)return json({error:"unauthorized"},401);const b=await context.request.json().catch(()=>null),publicKey=String(b?.publicKey||"");if(!publicKey)return json({error:"publicKey required"},400);const id=crypto.randomUUID(),now=Date.now();await context.env.DB.prepare("INSERT INTO devices(id,user_id,name,public_key,created_at,last_seen_at) VALUES(?,?,?,?,?,?)").bind(id,uid,String(b?.name||"device").slice(0,80),publicKey,now,now).run();return json({deviceId:id,publicKey});}
